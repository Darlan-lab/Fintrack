import type {   Request, 
                Response,
                NextFunction } from "express";


export class Auth {

    /*====================
        POST USER 
    ======================*/

    static async validarUser(req: Request, res: Response, next: NextFunction) {
        
    }

}